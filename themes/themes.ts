import { createTokens } from 'tamagui';
import { Colours } from '@/src/constants/colours';
export const lightColours = createTokens({
	color: Colours.light,
});

export const darkColours = createTokens({
	color: Colours.dark,
});
